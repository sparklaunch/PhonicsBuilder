import SwiftUI

struct SeaElephant: View {
    var body: some View {
        VStack(spacing: 0) {
            Spacer()
            HStack(spacing: 0) {
                Image("SeaElephant")
                    .scaleEffect(2.0)
                Spacer()
            }
        }
    }
}
