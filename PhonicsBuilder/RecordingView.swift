import SwiftUI

struct RecordingView: View {
    var body: some View {
        ZStack {
            Color.white.opacity(0.8)
            VStack {
                SpeakNowText()
                Spacer()
            }
        }
    }
}
